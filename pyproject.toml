[project]
name = "smart-translator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "a.zaynagabdinov", email = "The.scorpion2006@yandex.ru" }
]
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[tool.fastapi]
entrypoint = "main:app"
